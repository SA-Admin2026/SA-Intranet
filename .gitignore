__pycache__/
*.pyc
.DS_Store
node_modules/
dist/
.astro/
# public/ IS committed: assemble-content.mjs curates the media there (all files <= 10 MB —
# every image, PDF, doc — ship in-repo; the few large PowerPoint decks are held back and
# their links stubbed). See scripts/assemble-content.mjs + spaces/MEDIA-DEFERRED.md.
# The raw source attachments (full-size, all files) stay git-ignored:
spaces/*/attachments/
.claude/
