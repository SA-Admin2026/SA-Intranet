# Attachments deferred (Phase 4)

To keep this repo lean and within Cloudflare Pages' 25 MB/file limit, the migrated
`attachments/` folders are **git-ignored** for now (they remain on disk locally and in full
in the per-space staging repos: SA-Admin-Docs, SA-Offerings, SA-Internal-Systems, SA-DCA-Docs).

At Phase 4, fold media back in with a hosting decision: small images in-repo, large media
(PowerPoints, video) to SharePoint/Stream and linked. Four files already exceed 10 MB
(three Administrative summit .pptx up to 26 MB, one Internal Systems .pptx).
