---
// Unified build: every section lives in the one Starlight sidebar, so the header
// only needs the wordmark linking home. Kept as a component (not the default title)
// so the SA wordmark styling and the green full-stop stay consistent everywhere.
---
<div class="sa-sitenav">
  <a class="sa-home" href="/">semantic arts<b>.</b></a>
  <span class="sa-sub">intranet</span>
</div>
